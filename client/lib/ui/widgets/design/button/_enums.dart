part of 'button.dart';

enum AppButtonStyle {
  dark,
  blue,
  ghost,
  white,
  danger,
}

enum AppButtonState {
  elevated,
  disabled,
  plain,
}

enum AppButtonRadius {
  less,
  medium,
  round,
  capsule,
}

enum AppButtonSize {
  tiny,
  mini,
  small,
  medium,
  large,
}
