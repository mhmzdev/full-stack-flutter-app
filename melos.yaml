name: hop_maldives

scripts:
  pub-get:
    exec: flutter pub get
  flc:
    exec: flutter clean && flutter pub get
  
  pub-upgrade:
    exec: flutter pub upgrade --major-versions

packages:
  - client/
  - server/
  - server/packages/*